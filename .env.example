# Ejemplo de archivo .env para configuración de PostgreSQL
# Copia este archivo a `.env` (en la raíz) y completa con tus valores reales.
# NO subas .env a control de versiones (ya está en .gitignore).

# SECRET_KEY de Django (mantener privado)
SECRET_KEY=tu_secret_key_aqui

# Debug (True/False)
DEBUG=True

# Configuración de PostgreSQL
DB_NAME=proyecto_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
